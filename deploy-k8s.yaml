---
  - hosts: localhost
    become: yes
     roles:
      - "ec2key"
      - "ec2inst"

  - hosts: "tag_Env_development"
    become: yes
    roles:
      - "common"

 - hosts: tag_Role_master
    roles:
      - "k8s-master"

  - hosts: tag_Role_client
    roles:
      - "k8s-slaves"